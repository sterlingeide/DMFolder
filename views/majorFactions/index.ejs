<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Major Factions</title>
</head>
<body>
    <a href="/campaigns/<%= campaignId %>">View Campaign </a><br>
    <% for( let index = 0; index < majorFactions.length; index++ ) { %>
        <% let a = majorFactions[index] %> 
        <% a = a.toJSON() %>
        <p> <%= a.name %></p>
        <a href="/majorFactions/s/<%= a.id %>">View Major Faction </a><br>
        <a href="/majorFactions/edit/<%= a.id %>"> Edit</a>
        <form method="POST" action="/majorFactions/<%= a.id %>/?_method=DELETE">
            <input type="submit" value="Delete Major Faction">
        </form>
        <hr>
    <% } %>
    <a href="/majorFactions/new/<%= campaignId %>"> New Major Factions</a>
</body>
</html>